================================================================================

[14] 文件: traversal/templates/traversal/graph.html / traversal/views.py / traversal/templates/traversal/index.html
--------------------------------------------------------------------------------
变更摘要:
- graph 页面进一步简化：
  - 删除右侧独立“线路事件图例面板”与一般站点分组交互；
  - 将“线路显示 + 点击触发”合并到主图区右侧图例区域（同区展示与触发一体）；
  - 站点交互仅保留“换乘站点列表”，列表即展示面板且可直接点击高亮对应换乘站；
  - 保留主图缩放与清除高亮能力。
- algorithm 再次修复“拒绝连接”：
  - `/algorithm/` 直接渲染 `index.html`，彻底去除 iframe 链路。
- 在算法页面（index）头部增加返回入口：
  - 引导页 `/`
  - 拓扑页 `/graph/`

原因与效果:
- 满足“图例触发与显示合一、换乘站面板展示触发合一、页面简化”的要求；
- 避免浏览器 iframe 导致的 127.0.0.1 拒绝连接问题；
- 算法页可直接回到引导页与拓扑页。

================================================================================

[12] 文件: traversal/templates/traversal/graph.html
--------------------------------------------------------------------------------
变更摘要:
- 主图新增缩放交互（按钮 + Ctrl+滚轮）：支持放大、缩小、重置。
- 新增“线路图例点击高亮”：
  - 点击某线路调用 `/api/network/highlight/?d_sl=<line>`；
  - 对应线路高亮，其余线路灰暗；
  - 提供“清除线路高亮”恢复主图。
- 新增“站点分组高亮”事件：
  - 一般站点、两线路换乘站、三线路换乘站可点击触发；
  - 基于 `/api/network/` 节点数据在主图叠加彩色点；
  - 普通站点采用单线路色，换乘站采用多线路 conic-gradient 色环。

原因与效果:
- 满足 graph 页面缩放、图例联动、分组站点高亮三项交互需求。

================================================================================

[13] 文件: traversal/views.py / traversal/templates/traversal/algorithm.html
--------------------------------------------------------------------------------
变更摘要:
- `/algorithm/` 恢复渲染 `algorithm.html`（不再重定向）。
- `algorithm.html` 保持与 guide/graph 一致白色简约风。
- 保留算法功能承载（iframe `/app/`），并注入同源样式：
  - 隐藏缩略图（`.thumb-map`）；
  - 将功能页主要区域调整为浅色主题风格；
  - 主图区保留图例可见（图例由后端图像渲染，始终彩色）。

原因与效果:
- 满足 algorithm 页面“统一风格 + 取消缩略图 + 图例保持彩色展示”的要求，同时保留原算法功能完整性。

================================================================================

[11] 文件: traversal/views.py
--------------------------------------------------------------------------------
变更摘要:
- `algorithm_page` 由渲染 `algorithm.html`（iframe 内嵌 `/app/`）改为直接重定向到 `/app/`。
- 同步引入 `redirect`。

原因与效果:
- 修复“点击算法页面出现 127.0.0.1 拒绝连接”的问题；规避不同浏览器/安全策略下 iframe 可能触发的拦截与连接报错。

================================================================================
代码修改报告（自动生成）
时间: 2026-04-21（会话内批量提交）
================================================================================

[1] 文件: network.py
--------------------------------------------------------------------------------
变更摘要:
- 新增 _primary_line_for_station，用于由站名解析“所属首条线路”，支撑灰暗分层。
- 重写 _draw_network1_style：用 d_start_line/d_start_station/d_end_line/d_end_station 表示下拉状态，
  mark_start/mark_end 表示输入框解析后的起终点标记；下拉选站时仅相关线路灰暗、其余更淡，
  仅下拉线路未选站时多线路可同时高亮；仅输入站名而无下拉站时保持全路网彩色（避免误触“全图高亮”）。
- 下拉选定站增加橙色强调点；若该站已有绿/红标记则不再叠画。
- render_network1_topology_image 改为上述参数，并增加 dpi 参数（默认 300），供高亮接口使用较低 dpi。

原因与效果:
- 修复终点侧仅能通过错误的 line/station 传参间接表达、导致与起点逻辑不一致及无选站时全路网过亮的问题；
- 支持起终点双线/双站组合的视觉状态。

================================================================================

[2] 文件: traversal/views.py
--------------------------------------------------------------------------------
变更摘要:
- get_network_highlight：改为读取 d_sl、d_ss、d_el、d_es、m_s、m_e；站名经 search_station 解析为图中精确名称；
  使用 MD5 短键缓存文件名；调用 render_network1_topology_image(..., dpi=140) 缩短生成耗时。
- 新增 _resolve_station_for_highlight 辅助函数。

原因与效果:
- 与前端统一协议，避免旧版单组 line/station 无法表达终点线路的问题；
- 降低预览图 dpi 以减轻交互延迟。

================================================================================

[3] 文件: traversal/templates/traversal/index.html
--------------------------------------------------------------------------------
变更摘要:
- 主图区域增加 #main-hover-tooltip，#planned-map-img 增加 crosshair 光标。
- 抽取 nearestTopologyEdge / nearestTopologyNode、formatHoverStationHtml、bindTopologyMapHover；
  缩略图与主图均绑定悬停：靠近站点优先显示换乘多线路配色与站名，否则显示区间边信息。
- 用 refreshTopologyHighlight 统一从四个下拉与两个输入框构造查询并刷新主拓扑图；
  修正 end-line-select 变更时曾错误沿用起点下拉的问题；搜索建议点击与热门起点点击后同步刷新高亮。

原因与效果:
- 终点线路点击立即反映在高亮图上；双端选择与起点行为一致；
- 主图具备与缩略图同源的悬停反馈；换乘站 tooltip 展示各交叉线路及配色。

================================================================================

[4] 文件: outline.txt（新建）
--------------------------------------------------------------------------------
项目模块大纲，便于新成员理解目录与数据流。

================================================================================

[5] 文件: network.py（补充）
--------------------------------------------------------------------------------
变更摘要:
- 边与换乘楔形的着色判断改为「先 station_ctx，再 line_only，再其余暗淡」并行条件，
  避免已选起点站点时仅下拉终点线路无法彩色高亮的问题。

原因与效果:
- 满足「一端已选站、另一端仅选线路」的组合交互。

================================================================================

[6] 文件: traversal/views.py / traversal/urls.py / traversal/templates/traversal/guide.html
--------------------------------------------------------------------------------
变更摘要:
- `requirements.txt` 增加 `folium>=0.20.0` 依赖，确保环境可复现。
- 新增引导页视图 `guide`，首次进入项目根路径时展示大屏导览页面；原主页面改为 `/app/`。
- 使用 `folium` 基于 `station_coordinates_complete.txt` 与 `line_color.txt` 绘制城市地铁网络：
  - 按线路绘制彩色 PolyLine；
  - 普通站点绘制单色圆点；
  - 换乘站点使用 `DivIcon + conic-gradient` 色环表示交叉线路；
  - 增加站点/线路 tooltip 与左下图例。
- 新增统计面板计算逻辑（含球面距离估算）：
  - 线路数量、站点数量、普通站点数量；
  - 两线路换乘站点数量、三线路换乘站点数量；
  - 单线路平均里程、总线路里程。
- 引导页 UI 采用全屏 HUD 科技风，按钮布局：
  - 左侧“网络展示”：当前为占位提示（暂无功能）；
  - 右侧“算法展示”：跳转原主页面 `/app/`。

原因与效果:
- 满足“项目启动即进入引导交互页面”需求，并形成网络态势总览 + 数据展板 + 主功能入口的一体化首屏。

================================================================================
（以下可由后续修改继续追加）
================================================================================

[7] 文件: traversal/templates/traversal/guide.html
--------------------------------------------------------------------------------
变更摘要:
- 引导页改为极简白色现代风格，仅保留“标题 + 两个按钮”。
- 标题更新为：基于非完全图的地铁网络TSP算法对比与可视化。
- 左按钮文案“拓扑结构”，点击跳转 `/graph/`。
- 右按钮文案“TSP算法”，点击跳转 `/algorithm/`。

原因与效果:
- 满足“guide 仅保留两个按钮和标题”的结构要求，并形成简洁统一入口。

================================================================================

[8] 文件: traversal/views.py / traversal/urls.py
--------------------------------------------------------------------------------
变更摘要:
- 新增页面视图：
  - `graph`：渲染 `graph.html`，提供主拓扑图、缩略地图与图结构数据；
  - `algorithm_page`：渲染 `algorithm.html`，承载算法功能页外壳。
- `guide` 视图改为纯模板渲染，不再加载大屏统计数据。
- 新增 `_build_real_network_map_html`，基于站点坐标与线路配色生成浅色（Google 风格近似）地图缩略图。
- 路由调整：
  - `/` -> guide
  - `/graph/` -> graph
  - `/algorithm/` -> algorithm
  - `/app/` -> 原 index（功能保留）

原因与效果:
- 完成“引导页 -> 拓扑页/算法页”的三级结构分流，同时保留原算法页面能力。

================================================================================

[9] 文件: traversal/templates/traversal/graph.html（新建）
--------------------------------------------------------------------------------
变更摘要:
- 新建拓扑展示页，整体白色简洁风，与 guide 风格一致。
- 主图：展示 `network.py` 生成的拓扑图（通过 `/api/file/` 读取输出图）。
- 右上角缩略图：显示基于 `station_coordinates_complete.txt` + `line_color.txt` 的真实坐标网络地图。
- 右侧数据区：展示图结构数据（节点数、边数、线路数、线路-站点 JSON、邻接表示节选）。

原因与效果:
- 满足“拓扑结构页面”要求，实现抽象图与真实网络并行对照展示，并输出图数据。

================================================================================

[10] 文件: traversal/templates/traversal/algorithm.html（新建）
--------------------------------------------------------------------------------
变更摘要:
- 新建算法对比页面外壳（白色简约风，与 guide/graph 一致）。
- 通过 iframe 嵌入 `/app/` 原页面，复用既有“算法对比 + 路径规划”完整功能。

原因与效果:
- 在保证统一视觉风格的同时，快速承接原 `index.html` 的算法功能，避免重复实现和回归风险。

================================================================================

[15] 文件: guide.py
--------------------------------------------------------------------------------
变更摘要:
- 将 `guide.py` 从“交互式绘图 + plt.show()”改为“生成 Django 模板 `traversal/templates/traversal/guide.html`”：
  - 生成包含 TSP 时间轴 + T/S/P 词云的 Banner，并以 PNG base64 data URI 内嵌进模板；
  - `P` 词云改为“仅下半部分放词 + 字母完整轮廓底字”，形成“淹没到 P 腰部”视觉；
  - `GRAPH` 文本位置改为贴近坐标系底部绘制，使其底部与 `TSP` 词云底部对齐。
- 新增 `write_guide_template()`、`build_guide_template()` 等函数，脚本直接写入模板文件。

原因与效果:
- 满足“P 字母词云控制在 P 高度一半并淹没到腰部、Graph 底部对齐”的排版需求；
- 满足“guide.py 由生成图片改为生成 guide.html 文件”的输出形态，并为后续嵌入 Django 提供模板产物。

================================================================================

[16] 文件: traversal/views.py
--------------------------------------------------------------------------------
变更摘要:
- `guide()` 视图由“纯模板渲染”改为注入 `_build_guide_context()`：向 `guide.html` 提供 `map_html` 与统计字段（线路数、站点数、里程估算等）。

原因与效果:
- 引导页从单纯入口按钮升级为“Banner + 地铁网络地图 + 统计展板”的一体化首屏，保持项目整体嵌入与统一。

================================================================================

[17] 文件: traversal/templates/traversal/guide.html
--------------------------------------------------------------------------------
变更摘要:
- 由 `guide.py` 自动生成新的 `guide.html` 模板内容：
  - 首屏加入 TSP 时间轴 Banner（内嵌 base64 PNG）；
  - 保留原“拓扑结构 / TSP算法”按钮与跳转逻辑不变；
  - 增加地图区域与统计面板，读取后端注入的 `map_html` 与统计字段。

原因与效果:
- 满足“项目为一体”的引导首屏展示，同时保持原入口跳转行为。

================================================================================

[18] 文件: guide.py
--------------------------------------------------------------------------------
变更摘要:
- 重新实现 `P` 区域词云逻辑，满足“空心 P + 半高外围词云”：
  - 新增 `create_outer_lower_half_mask()`，仅允许字母外围且下半部分区域放词；
  - `P` 字母改为空心表现（白色内层 + 绿色描边叠加）；
  - 调整 `P` 词云字号范围与参数（减小 `max_font_size`、启用 `repeat`），确保关键词能全部写入词云区域。
- `GRAPH` 底部对齐改为渲染后按文本真实边界校正：
  - 使用 `get_window_extent` 计算字形底边；
  - 反算到轴坐标后再微调文本 y，保证与右侧词云底部水平一致。
- 精简 `guide.html` 生成模板：
  - 移除地图、统计、额外标题，仅保留 `@guide.py` 生成 Banner 与两个按钮。

原因与效果:
- 修复“Graph 与词云底部未对齐”；
- 满足“P 字母空心、词云仅到 P 半高、并确保词项完整进入词云”的视觉与内容要求；
- 满足“guide.html 保持简洁，仅保留生成内容和两个入口按钮”的页面要求。

================================================================================

[19] 文件: traversal/views.py
--------------------------------------------------------------------------------
变更摘要:
- `guide()` 视图恢复为纯模板渲染，不再注入地图统计上下文。

原因与效果:
- 与新的极简 `guide.html` 一致，避免无用数据构建，页面逻辑更简洁。

================================================================================

[20] 文件: guide.py
--------------------------------------------------------------------------------
变更摘要:
- 引导页模板改为全屏布局：
  - 新增 `.page` + `.wrap` 全屏容器，`hero` 占满可视区域；
  - Banner 图片采用 `object-fit: contain`，确保全屏显示同时不拉伸；
  - 按钮区固定在底部，风格与页面保持一致（浅色背景 + 统一边框阴影体系）。
- `P` 词云样式回归与 `T/S` 同风格（字母内部词云）：
  - `P` 使用字母内部 mask，仅保留下半高度填充；
  - 保留描边参数与色系，使视觉风格与 T/S 一致；
  - 通过缩小 `P` 词云字号上限并降低最小字号，提升词项完整写入概率。
- 修复词云导出错误：
  - `P` 词云改为与 T/S 一致的白底输出，避免 RGBA + contour 导致的维度冲突。

原因与效果:
- 满足“guide.html 全屏显示、按钮和页面风格一致”的要求；
- 满足“P 与 TS 风格一致，唯一差异是仅填充到半高，并适当缩小字号保证词尽量写入”的要求。
